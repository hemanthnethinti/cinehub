import 'package:flutter_test/flutter_test.dart';

void main() {
  // Phase 2 — Core only. No widget tests yet.
  // Widget tests will be added in Phase 3 (Auth) and onwards.
  test('placeholder — Phase 2 core has no widget tests', () {
    expect(true, isTrue);
  });
}
